# Support Bot v4 - Smart Bot Deploy

Умный бот поддержки с интеграцией OpenAI Assistant API, калькулятором логистики и умным определением запросов.

## 🚀 Быстрый старт

### Вариант 1: Простой запуск (рекомендуется)

**Linux/Mac:**
```bash
chmod +x quick_start.sh
./quick_start.sh
```

**Windows:**
```cmd
install.bat
run.bat
```

**Универсальный (любая ОС):**
```bash
python3 start.py
```

### Вариант 2: Docker (для продакшена)

```bash
# Создайте .env файл
cp env_example.txt .env
nano .env  # заполните настройки

# Запустите через Docker Compose
docker-compose up -d

# Просмотр логов
docker-compose logs -f
```

### Вариант 3: Ручная настройка

1. **Настройка окружения:**
```bash
cp env_example.txt .env
nano .env  # отредактируйте настройки
```

2. **Заполните настройки в .env:**
```env
# OpenAI Configuration
OPENAI_API_KEY=sk-proj-...
ASSISTANT_ID=asst_...

# Telegram Configuration  
TELEGRAM_API_ID=22432346
TELEGRAM_API_HASH=your_hash_here
PHONE_NUMBER=+79123456789

# Bot Configuration
BOT_NAME=support_bot_v4
```

3. **Запуск:**
```bash
# Linux/Mac
chmod +x run.sh && ./run.sh

# Windows
run.bat

# Или напрямую
pip install -r requirements.txt
python3 main.py
```

## 📁 Структура проекта

```
deploy_smart_bot/
├── main.py                 # Основной файл бота
├── file_manager.py         # Управление файлами
├── text_cleaner.py         # Очистка текста от меток
├── human_behavior.py       # Симуляция человеческого поведения
├── smart_file_detector.py  # Умное определение запросов
├── requirements.txt        # Зависимости Python
├── env_example.txt         # Пример конфигурации
├── run.bat                 # Скрипт запуска для Windows
├── run.sh                  # Скрипт запуска для Linux/Mac
├── README_DEPLOY.md        # Этот файл
├── Картинки/               # Папка с изображениями склада
│   ├── photo_*.jpg
│   └── *.mp4
└── ТЗ.xlsx                 # Файл технического задания
```

## 🧠 Возможности бота

### Умное определение запросов
- **Расчет логистики**: "носки 1000 штук, вес 100 грамм, ozon, москва"
- **Файл ТЗ**: "мне нужен файл ТЗ", "пришлите техническое задание"
- **Изображения склада**: "покажите склад", "как добраться до вас"
- **Обычное общение**: "привет", "как дела", "помогите"

### Интеграция с OpenAI
- Использует Assistant API для ответов
- Автоматическая очистка от меток source
- Работа с базой знаний через File Search

### Красивое форматирование
- Объединение адресов, ссылок и фото в одно сообщение
- Эмодзи и структурированная информация
- Человеческое поведение с паузами и реакциями

## 🔧 Настройка

### OpenAI Assistant
1. Создайте ассистента в OpenAI Platform
2. Настройте системный промпт
3. Подключите File Search с файлом addresses.txt
4. Скопируйте ASSISTANT_ID в .env

### Telegram Bot
1. Получите API_ID и API_HASH на https://my.telegram.org
2. Укажите номер телефона для авторизации
3. При первом запуске введите код подтверждения

## 📊 Мониторинг

Логи сохраняются в файл `bot.log`:
- Информация о запросах
- Ошибки и предупреждения
- Статистика работы

## 🛠️ Разработка

### Добавление новых функций
1. Создайте обработчик в `main.py`
2. Добавьте определение типа запроса в `smart_file_detector.py`
3. Обновите документацию

### Тестирование
```bash
python -m pytest tests/
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в `bot.log`
2. Убедитесь, что все настройки в `.env` корректны
3. Проверьте подключение к интернету
4. Убедитесь, что OpenAI API ключ активен

## 🔄 Обновления

Для обновления бота:
1. Остановите текущий процесс
2. Замените файлы на новые версии
3. Перезапустите бота

## 📝 Лицензия

Внутренний проект компании "Апельсин"
