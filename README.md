# Support Bot v4 - Инструкция по запуску

## 🚀 Быстрый старт

### Шаг 1: Установка Python

Убедитесь, что у вас установлен Python 3.8 или выше:
```bash
python --version
# или
python3 --version
```

### Шаг 2: Установка зависимостей

Зависимости установятся автоматически при первом запуске, или установите вручную:
```bash
pip install -r requirements.txt
```

### Шаг 3: Настройка конфигурации

1. **Создайте файл `.env`** (может быть создан автоматически из шаблона)

2. **Заполните обязательные настройки в `.env`:**

```env
# OpenAI Configuration (ОБЯЗАТЕЛЬНО)
OPENAI_API_KEY=ваш_api_ключ_openai
ASSISTANT_ID=ваш_assistant_id

# Telegram Configuration (ОБЯЗАТЕЛЬНО)
TELEGRAM_API_ID=ваш_telegram_api_id
TELEGRAM_API_HASH=ваш_telegram_api_hash

# Bot Configuration
BOT_NAME=support_bot_v4

# Optional
LOG_LEVEL=INFO
```

#### Где получить настройки:

**OpenAI API:**
1. Зарегистрируйтесь на https://platform.openai.com
2. Перейдите в API Keys и создайте новый ключ
3. Скопируйте ключ в `OPENAI_API_KEY`

**OpenAI Assistant ID:**
1. Создайте Assistant в OpenAI Platform
2. Скопируйте Assistant ID в `ASSISTANT_ID`

**Telegram API:**
1. Перейдите на https://my.telegram.org/apps
2. Войдите с номером телефона
3. Создайте приложение
4. Скопируйте `api_id` и `api_hash`

### Шаг 4: Запуск бота

Просто запустите:
```bash
python start.py
```

Или на Linux/Mac:
```bash
python3 start.py
```

---

## 📋 Структура проекта

```
project/
├── main.py                    # Главный файл бота
├── start.py                   # Скрипт запуска
├── config/                    # Конфигурация
│   └── settings.py
├── infrastructure/            # Инфраструктура
│   └── storage/              # Хранилища данных
├── domain/                    # Бизнес-логика
├── requirements.txt           # Зависимости
├── env_example.txt           # Пример конфигурации
└── .env                      # Ваши настройки (создается автоматически)
```

---

## ⚠️ Важные замечания

1. **Файл `.env` обязателен** - без него бот не запустится
2. **API ключи должны быть валидными** - проверьте их перед запуском
3. **При первом запуске Telegram** попросит ввести код подтверждения
4. **Логи сохраняются** в файл `bot.log`

---

## 🔧 Решение проблем

### Ошибка "OPENAI_API_KEY не найден"
- Убедитесь, что файл `.env` существует
- Проверьте, что переменная `OPENAI_API_KEY` заполнена в `.env`

### Ошибка "ModuleNotFoundError"
- Установите зависимости: `pip install -r requirements.txt`

### Ошибка при запуске Telegram
- Проверьте правильность `TELEGRAM_API_ID` и `TELEGRAM_API_HASH`
- Убедитесь, что номер телефона указан корректно

---

## 📞 Поддержка

При возникновении проблем проверьте логи в файле `bot.log`

